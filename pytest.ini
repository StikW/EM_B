[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra -p no:cacheprovider
markers =
    backend: pruebas del backend Express
    unit: pruebas unitarias de registro y login
    functional: pruebas funcionales de registro y login
